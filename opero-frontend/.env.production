# Configurazione PRODUZIONE
# NON impostare REACT_APP_API_BASE_URL qui!
# Il frontend userà automaticamente il percorso relativo '/api'
# che verrà servito dallo stesso dominio (nginx reverse proxy)

# Porta del Gestionale React (solo per sviluppo locale)
PORT=3002

# Disabilita source maps in produzione per sicurezza
GENERATE_SOURCEMAP=false
